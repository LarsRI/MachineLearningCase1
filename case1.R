## ---- chunkname
# you have to start a chunk with ## ----
# and then add the name of the chunk, if you want to have it in another file
# then the file has to be read by read_chunk in the beginning
1 + 1
plot(seq(5),seq(5))